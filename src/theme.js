import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ebe6e4ff', // Change this to your primary color
    },
    secondary: {
      main: '#c4d3ccff', // Change this to your secondary color
    },
  },
});

export default theme;
